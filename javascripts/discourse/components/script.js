document.addEventListener('DOMContentLoaded', function() {
    var hideButton = document.getElementById('hide');
    if (hideButton) {
        hideButton.addEventListener('click', hideIntro);
    }
});
